import styled from "styled-components";
import stylers from "@paprika/stylers";

export const Content = styled.div`
  box-sizing: border-box;
  padding: ${stylers.spacer(1)} ${stylers.spacer(2)} ${stylers.spacer(2)} ${stylers.spacer(2)};
  width: 100%;
`;
