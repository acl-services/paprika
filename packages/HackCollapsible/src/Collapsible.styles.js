import styled, { css } from "styled-components";
import { theme } from "@paprika/themes";

export const Collapsible = styled.div(theme("Collapsible", () => css``));
export const CollapsibleBody = styled.div(theme("CollapsibleBody", () => css``));
export const CollapsibleIcon = styled.span(theme("CollapsibleIcon", () => css``));
