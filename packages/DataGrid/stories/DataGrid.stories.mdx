import { Meta } from "@storybook/addon-docs/blocks";
import MDX from "./DataGrid.mdx";
import DataGrid from "../src";

export const params = {
  options: {
    isToolshown: true, 
    showPanel: false,
  },
}

<Meta title="DataGrid/ Docs" component={DataGrid} parameters={params} />

<MDX />
