import { Meta } from "@storybook/addon-docs/blocks";
import { getStoryName } from "storybook/storyTree";
import MDX from "./NotificationCard.mdx";
import NotificationCard from "../src";

export const params = {
  options: {
    isToolshown: true,
    showPanel: false,
  },
};

<Meta title={`${getStoryName("NotificationCard")}/Docs`} component={NotificationCard} parameters={params} />

<MDX />
