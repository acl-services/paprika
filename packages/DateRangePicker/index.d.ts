export default DateRangePicker;

declare function DateRangePicker(props: any): JSX.Element;
declare namespace propTypes {
  export {};
  const startDate: instanceOf;
  const endDate: instanceOf;
  const onChange: func;
  const children: node;
}
