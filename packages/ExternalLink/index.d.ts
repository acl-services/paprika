export default ExternalLink;

declare function ExternalLink(props: any): JSX.Element;
declare namespace propTypes {
  export {};
  const ariaText: string;
  const children: string;
  const hasNoUnderline: bool;
  const href: string;
}
