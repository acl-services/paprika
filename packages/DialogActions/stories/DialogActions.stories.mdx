import { Meta } from "@storybook/addon-docs/blocks";
import { getStoryName } from "storybook/storyTree";
import MDX from "./DialogActions.mdx";
import DialogActions from "../src";

export const params = {
  options: {
    isToolshown: true,
    showPanel: false,
  },
};

<Meta title={`${getStoryName("DialogActions")}/Docs`} component={DialogActions} parameters={params} />

<MDX />
