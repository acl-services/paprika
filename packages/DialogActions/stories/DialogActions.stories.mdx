import { Meta } from "@storybook/addon-docs/blocks";
import MDX from "./DialogActions.mdx";
import DialogActions from "../src";

export const params = {
  options: {
    isToolshown: true,
    showPanel: false,
  },
};

<Meta title="DialogActions/ Docs" component={DialogActions} parameters={params} />

<MDX />
