import { Meta} from "@storybook/addon-docs/blocks";
import MDX from "./Toast.mdx";
import Toast from "../src";

export const params = {
  options: {
    isToolshown: true, 
    showPanel: false,
  },
}

<Meta title="Toast/ Docs" component={Toast} parameters={params} />

<MDX />
