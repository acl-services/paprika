import { Meta } from "@storybook/addon-docs/blocks";
import { getStoryName } from "storybook/storyTree";
import MDX from "./ListBox.mdx";
import ListBox from "../src/";

export const params = {
  options: {
    isToolshown: true,
    showPanel: false,
  },
};

<Meta title={`${getStoryName("ListBox")}/Docs`} component={ListBox} parameters={params} />

<MDX />
