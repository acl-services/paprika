import { Meta } from "@storybook/addon-docs/blocks";
import MDX from "./ListBox.mdx";
import ListBox from "../src/";

export const params = {
  options: {
    isToolshown: true, 
    showPanel: false,
  },
}

<Meta title="ListBox/ Docs" component={ListBox} parameters={params} />

<MDX />
