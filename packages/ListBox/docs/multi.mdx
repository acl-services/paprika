---
name: Multiple
route: "/listbox/multiple"
menu: "Listbox"
---

# Multiple selection

<!------------------- IMPORTS ------------------>

import ListBox from "../src";
import { Playground, PropsTable } from "docz";

## Getting started

```sh
  npm install @paprika/listbox
```

or

```sh
  yarn add @paprika/listbox
```

## Examples

### Simple

<Playground>
  <ListBox isMulti zIndex={10000} getScrollContainer={() => document.querySelector("#root > div")}>
    <ListBox.Option>Spiderman</ListBox.Option>
    <ListBox.Option>Venom</ListBox.Option>
    <ListBox.Option>Carnage</ListBox.Option>
  </ListBox>
</Playground>

```js
<ListBox isMulti>
  <ListBox.Option>Spiderman</ListBox.Option>
  <ListBox.Option>Venom</ListBox.Option>
  <ListBox.Option>Carnage</ListBox.Option>
</ListBox>
```

### With Confirmation

<Playground>
  <ListBox isMulti zIndex={10000} getScrollContainer={() => document.querySelector("#root > div")}>
    <ListBox.Option>Spiderman</ListBox.Option>
    <ListBox.Option>Venom</ListBox.Option>
    <ListBox.Option>Carnage</ListBox.Option>
    <ListBox.Option>Doctor Octopus</ListBox.Option>
    <ListBox.Option>Batman</ListBox.Option>
    <ListBox.Option>Joker</ListBox.Option>
    <ListBox.Option>Scarecrow</ListBox.Option>
    <ListBox.Option>Two-Face</ListBox.Option>
    <ListBox.Footer />
  </ListBox>
</Playground>

```js
<ListBox isMulti>
  <ListBox.Option>Spiderman</ListBox.Option>
  <ListBox.Option>Venom</ListBox.Option>
  <ListBox.Option>Carnage</ListBox.Option>
  <ListBox.Option>Doctor Octopus</ListBox.Option>
  <ListBox.Option>Batman</ListBox.Option>
  <ListBox.Option>Joker</ListBox.Option>
  <ListBox.Option>Scarecrow</ListBox.Option>
  <ListBox.Option>Two-Face</ListBox.Option>
  <ListBox.Footer />
</ListBox>
```

### With filter

<Playground>
  <ListBox isMulti zIndex={10000} hasFilter getScrollContainer={() => document.querySelector("#root > div")}>
    <ListBox.Option>Spiderman</ListBox.Option>
    <ListBox.Option>Venom</ListBox.Option>
    <ListBox.Option>Carnage</ListBox.Option>
    <ListBox.Option>Doctor Octopus</ListBox.Option>
    <ListBox.Option>Batman</ListBox.Option>
    <ListBox.Option>Joker</ListBox.Option>
    <ListBox.Option>Scarecrow</ListBox.Option>
    <ListBox.Option>Two-Face</ListBox.Option>
  </ListBox>
</Playground>

```js
<Playground>
  <ListBox isMulti hasFilter>
    <ListBox.Option>Spiderman</ListBox.Option>
    <ListBox.Option>Venom</ListBox.Option>
    <ListBox.Option>Carnage</ListBox.Option>
    <ListBox.Option>Doctor Octopus</ListBox.Option>
    <ListBox.Option>Batman</ListBox.Option>
    <ListBox.Option>Joker</ListBox.Option>
    <ListBox.Option>Scarecrow</ListBox.Option>
    <ListBox.Option>Two-Face</ListBox.Option>
  </ListBox>
</Playground>
```

### With Groups

<Playground>
  <ListBox isMulti zIndex={10000} getScrollContainer={() => document.querySelector("#root > div")}>
    <ListBox.Group groupId="spidermanUniverse" label="Spiderman Universe">
      <ListBox.Option>Spiderman</ListBox.Option>
      <ListBox.Option>Venom</ListBox.Option>
      <ListBox.Option>Carnage</ListBox.Option>
      <ListBox.Option>Doctor Octopus</ListBox.Option>
    </ListBox.Group>
    <ListBox.Group groupId="batmanUniverse" label="Batman Universe">
      <ListBox.Option>Batman</ListBox.Option>
      <ListBox.Option>Joker</ListBox.Option>
      <ListBox.Option>Scarecrow</ListBox.Option>
      <ListBox.Option>Two-Face</ListBox.Option>
    </ListBox.Group>
  </ListBox>
</Playground>

```js
<ListBox isMulti>
  <ListBox.Group groupId="spidermanUniverse" label="Spiderman Universe">
    <ListBox.Option>Spiderman</ListBox.Option>
    <ListBox.Option>Venom</ListBox.Option>
    <ListBox.Option>Carnage</ListBox.Option>
    <ListBox.Option>Doctor Octopus</ListBox.Option>
  </Listbox.Group>
  <ListBox.Group groupId="batmanUniverse" label="Batman Universe">
    <ListBox.Option>Batman</ListBox.Option>
    <ListBox.Option>Joker</ListBox.Option>
    <ListBox.Option>Scarecrow</ListBox.Option>
    <ListBox.Option>Two-Face</ListBox.Option>
  </ListBox.Group>
</ListBox>
```
