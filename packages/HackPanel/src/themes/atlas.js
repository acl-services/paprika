import { css } from "styled-components";

export const Panel = {
  Panel: () => css`
    [data-pka-anchor="panel.header"] {
      height: auto;
      width: auto;
    }

    [data-pka-anchor="heading"] {
      font-weight: 400;
    }
  `,
};
