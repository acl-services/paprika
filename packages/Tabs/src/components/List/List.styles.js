import styled from "styled-components";
import tokens from "@paprika/tokens";

export const tabsListStyles = styled.div`
  border-bottom: 1px solid ${tokens.color.creme};
  margin: 0;
  padding: 0;
  position: relative;
  width: 100%;
`;
