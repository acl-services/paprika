import { align } from "@paprika/constants";

export default {
  NUMBER: "number",
  STRING: "string",
  DATE: "date",
  align: {
    LEFT: align.LEFT,
    RIGHT: align.RIGHT,
    CENTER: align.CENTER,
  },
};
