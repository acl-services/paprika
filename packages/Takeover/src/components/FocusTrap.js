import Overlay from "@paprika/overlay";

const propTypes = Overlay.propTypes.focusTrapOptions;

const defaultProps = Overlay.defaultProps.focusTrapOptions;

const FocusTrap = () => null;

FocusTrap.propTypes = propTypes;
FocusTrap.defaultProps = defaultProps;
FocusTrap.displayName = "Takeover.FocusTrap";

export default FocusTrap;
