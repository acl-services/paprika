import { Meta } from "@storybook/addon-docs/blocks";
import MDX from "./SidePanel.mdx";
import SidePanel from "../src";

export const params = {
  options: {
    isToolshown: true,
    showPanel: false,
  },
};

<Meta title=" | SidePanel/ Docs" component={SidePanel} parameters={params} />

<MDX />
