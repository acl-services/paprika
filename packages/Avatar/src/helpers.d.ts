import React from "react";
export declare function getAvatarColors(children: React.ReactNode): {
    fontColor: string;
    backgroundColor: string;
};
export declare function getInitialsFromText(text: string, length?: number): string;
