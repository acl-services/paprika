import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';

import CollapsibleText from "../src";


<Meta title="MDX|CollapsibleText" component={CollapsibleText} />

# CollapsibleText

Let's define a story for our `Badge` component:

<Story name="positive">
  <CollapsibleText>Lorem hipsum raw denim listicle mixtape.</CollapsibleText>
</Story>

We can drop it in a `Preview` to get a code snippet:

<Preview>
  <Story name="negative">
    <CollapsibleText>Lorem hipsum raw denim listicle mixtape.</CollapsibleText>
  </Story>
</Preview>
