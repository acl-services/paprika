import { Meta, Props } from '@storybook/addon-docs/blocks';
import CollapsibleText from "../src";

export const params = {
  options: {
    isToolshown: true, 
    showPanel: false,
  },
}

<Meta title="CollapsibleText/ Props" component={CollapsibleText} parameters={params} />

# &lt;CollapsibleText&gt; Props

<Props of={CollapsibleText} />

