import tokens from "@paprika/tokens";
import stylers from "@paprika/stylers";

const collapsibleChecklistsStyles = props => `
`;

// background-color: ${tokens.color.orange};

export default collapsibleChecklistsStyles;
