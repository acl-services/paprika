import { Meta } from "@storybook/addon-docs/blocks";
import { getStoryName } from "storybook/storyTree";
import MDX from "./SideNavigation.mdx";
import SideNavigation from "../src";

export const params = {
  options: {
    isToolshown: true,
    showPanel: false,
  },
};

<Meta title={`${getStoryName("SideNavigation")}/Docs`} component={SideNavigation} parameters={params} />

<MDX />
