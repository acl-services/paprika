name: Paprika CI Workflow

on:
  pull_request:
  push:
    branches:
      - master

# Cancel in-progress runs for PRs to only run latest, but allow all runs queued for master branch
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read
  pull-requests: write # Needed to update PR body with Storybook link

jobs:
  check-packages:
    name: 🔍 Check packages changed
    runs-on: ubuntu-latest
    outputs:
      packages-changed: ${{ steps.check-packages.outputs.changed }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # Only fetch enough history for path filtering (2 commits for push, base+head for PR)
          fetch-depth: ${{ github.event_name == 'pull_request' && '0' || '2' }}

      - name: Check for changes in packages
        id: check-packages
        run: |
          if [ "${{ github.event_name }}" == "pull_request" ]; then
            # For PRs, fetch base branch and compare
            BASE_REF="${{ github.event.pull_request.base.ref }}"
            git fetch --depth=1 origin "$BASE_REF"
            BASE_SHA="origin/$BASE_REF"
            HEAD_SHA="${{ github.sha }}"
            if git diff --name-only "$BASE_SHA" "$HEAD_SHA" --quiet -- packages/ 2>/dev/null; then
              echo "changed=false" >> $GITHUB_OUTPUT
            else
              echo "changed=true" >> $GITHUB_OUTPUT
            fi
          else
            # For pushes, compare against previous commit
            if git rev-parse --verify HEAD~1 >/dev/null 2>&1; then
              if git diff --name-only HEAD~1 HEAD --quiet -- packages/; then
                echo "changed=false" >> $GITHUB_OUTPUT
              else
                echo "changed=true" >> $GITHUB_OUTPUT
              fi
            else
              # First commit on branch, assume packages changed
              echo "changed=true" >> $GITHUB_OUTPUT
            fi
          fi

  setup:
    name: 🎩 Yarn & caching
    needs: check-packages
    runs-on: ubuntu-latest
    steps:
      - name: Skip if no package changes
        if: needs.check-packages.outputs.packages-changed != 'true'
        run: |
          echo "✅ No package changes detected, skipping setup"
          exit 0

      - name: Checkout repository
        if: needs.check-packages.outputs.packages-changed == 'true'
        uses: actions/checkout@v4

      - name: Setup Node.js
        if: needs.check-packages.outputs.packages-changed == 'true'
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: "yarn"

      - name: Setup Yarn
        if: needs.check-packages.outputs.packages-changed == 'true'
        run: corepack enable

      - name: Cache node_modules
        if: needs.check-packages.outputs.packages-changed == 'true'
        uses: actions/cache@v4
        id: node-modules-cache
        with:
          path: |
            node_modules
            **/node_modules
          # Match Semaphore cache strategy: branch-specific with yarn.lock hash, then branch-specific, then master
          key: ${{ runner.os }}-node-modules-${{ github.ref_name }}-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-node-modules-${{ github.ref_name }}-
            ${{ runner.os }}-node-modules-master-
            ${{ runner.os }}-node-modules-

      - name: Cache Cypress
        if: needs.check-packages.outputs.packages-changed == 'true'
        uses: actions/cache@v4
        with:
          path: ~/.cache/Cypress
          # Match Semaphore: branch-specific cache
          key: ${{ runner.os }}-cypress-${{ github.ref_name }}
          restore-keys: |
            ${{ runner.os }}-cypress-master
            ${{ runner.os }}-cypress-

      - name: Cache Lerna
        if: needs.check-packages.outputs.packages-changed == 'true'
        uses: actions/cache@v4
        with:
          path: |
            .lerna
            **/.lerna
          key: ${{ runner.os }}-lerna-${{ github.ref_name }}-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-lerna-${{ github.ref_name }}-
            ${{ runner.os }}-lerna-master-
            ${{ runner.os }}-lerna-

      - name: Install dependencies
        if: needs.check-packages.outputs.packages-changed == 'true' && steps.node-modules-cache.outputs.cache-hit != 'true'
        run: yarn install --frozen-lockfile

      - name: Bootstrap Lerna
        if: needs.check-packages.outputs.packages-changed == 'true'
        run: npx lerna bootstrap

  storybook-link:
    name: 🔗 Storybook link
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && (github.event.action == 'opened' || github.event.action == 'synchronize')
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "yarn"

      - name: Setup Yarn
        run: corepack enable

      - name: Install @octokit/rest
        run: |
          # Only install @octokit/rest which is needed for the script
          yarn add -W --no-lockfile --non-interactive @octokit/rest

      - name: Insert Storybook link into PR Body Message
        env:
          # Use built-in GITHUB_TOKEN (automatically provided, doesn't expire)
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Branch name for Storybook URL
          # For PRs, use head_ref (source branch); for pushes, use ref_name
          GITHUB_BRANCH_NAME: ${{ github.head_ref || github.ref_name }}
        run: node scripts/storybookLink.js

  format:
    name: 💅 Format
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: "yarn"

      - name: Setup Yarn
        run: corepack enable

      - name: Cache node_modules
        uses: actions/cache@v4
        id: node-modules-cache
        with:
          path: |
            node_modules
            **/node_modules
          # Match Semaphore cache strategy: branch-specific with yarn.lock hash, then branch-specific, then master
          key: ${{ runner.os }}-node-modules-${{ github.ref_name }}-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-node-modules-${{ github.ref_name }}-
            ${{ runner.os }}-node-modules-master-
            ${{ runner.os }}-node-modules-

      - name: Install dependencies
        if: steps.node-modules-cache.outputs.cache-hit != 'true'
        run: yarn install --frozen-lockfile

      - name: Lint && Prettier
        run: |
          yarn lint && yarn prettier -- --list-different

  accessibility:
    name: ♿️ Accessibility
    needs: [check-packages, setup]
    runs-on: ubuntu-latest
    steps:
      - name: Skip if no package changes
        if: needs.check-packages.outputs.packages-changed != 'true'
        run: |
          echo "✅ No package changes detected, skipping accessibility tests"
          exit 0
      - name: Checkout repository
        if: needs.check-packages.outputs.packages-changed == 'true'
        uses: actions/checkout@v4

      - name: Setup Node.js
        if: needs.check-packages.outputs.packages-changed == 'true'
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: "yarn"

      - name: Setup Yarn
        if: needs.check-packages.outputs.packages-changed == 'true'
        run: corepack enable

      - name: Cache node_modules
        if: needs.check-packages.outputs.packages-changed == 'true'
        uses: actions/cache@v4
        id: node-modules-cache
        with:
          path: |
            node_modules
            **/node_modules
          # Match Semaphore cache strategy: branch-specific with yarn.lock hash, then branch-specific, then master
          key: ${{ runner.os }}-node-modules-${{ github.ref_name }}-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-node-modules-${{ github.ref_name }}-
            ${{ runner.os }}-node-modules-master-
            ${{ runner.os }}-node-modules-

      - name: Cache Lerna
        if: needs.check-packages.outputs.packages-changed == 'true'
        uses: actions/cache@v4
        id: lerna-cache
        with:
          path: |
            .lerna
            **/.lerna
          key: ${{ runner.os }}-lerna-${{ github.ref_name }}-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-lerna-${{ github.ref_name }}-
            ${{ runner.os }}-lerna-master-
            ${{ runner.os }}-lerna-

      - name: Install dependencies
        if: needs.check-packages.outputs.packages-changed == 'true' && (steps.node-modules-cache.outputs.cache-hit != 'true' || steps.lerna-cache.outputs.cache-hit != 'true')
        run: yarn install --frozen-lockfile

      - name: Bootstrap Lerna
        if: needs.check-packages.outputs.packages-changed == 'true'
        run: npx lerna bootstrap

      - name: Axe test
        if: needs.check-packages.outputs.packages-changed == 'true'
        run: yarn test:a11y

  test:
    name: ⚡️ Integration/Unit
    needs: [check-packages, setup]
    runs-on: ubuntu-latest
    steps:
      - name: Skip if no package changes
        if: needs.check-packages.outputs.packages-changed != 'true'
        run: |
          echo "✅ No package changes detected, skipping unit/integration tests"
          exit 0
      - name: Checkout repository
        if: needs.check-packages.outputs.packages-changed == 'true'
        uses: actions/checkout@v4

      - name: Setup Node.js
        if: needs.check-packages.outputs.packages-changed == 'true'
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: "yarn"

      - name: Setup Yarn
        if: needs.check-packages.outputs.packages-changed == 'true'
        run: corepack enable

      - name: Cache node_modules
        if: needs.check-packages.outputs.packages-changed == 'true'
        uses: actions/cache@v4
        id: node-modules-cache
        with:
          path: |
            node_modules
            **/node_modules
          # Match Semaphore cache strategy: branch-specific with yarn.lock hash, then branch-specific, then master
          key: ${{ runner.os }}-node-modules-${{ github.ref_name }}-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-node-modules-${{ github.ref_name }}-
            ${{ runner.os }}-node-modules-master-
            ${{ runner.os }}-node-modules-

      - name: Cache Lerna
        if: needs.check-packages.outputs.packages-changed == 'true'
        uses: actions/cache@v4
        with:
          path: |
            .lerna
            **/.lerna
          key: ${{ runner.os }}-lerna-${{ github.ref_name }}-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-lerna-${{ github.ref_name }}-
            ${{ runner.os }}-lerna-master-
            ${{ runner.os }}-lerna-

      - name: Install dependencies
        if: needs.check-packages.outputs.packages-changed == 'true' && steps.node-modules-cache.outputs.cache-hit != 'true'
        run: yarn install --frozen-lockfile

      - name: Bootstrap Lerna
        if: needs.check-packages.outputs.packages-changed == 'true'
        run: npx lerna bootstrap

      - name: JEST
        if: needs.check-packages.outputs.packages-changed == 'true'
        run: yarn test

  e2e:
    name: ⛰ E2E
    needs: [check-packages, setup]
    runs-on: ubuntu-latest
    steps:
      - name: Skip if no package changes
        if: needs.check-packages.outputs.packages-changed != 'true'
        run: |
          echo "✅ No package changes detected, skipping E2E tests"
          exit 0
      - name: Checkout repository
        if: needs.check-packages.outputs.packages-changed == 'true'
        uses: actions/checkout@v4

      - name: Setup Node.js
        if: needs.check-packages.outputs.packages-changed == 'true'
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: "yarn"

      - name: Setup Yarn
        if: needs.check-packages.outputs.packages-changed == 'true'
        run: corepack enable

      - name: Cache apt packages
        if: needs.check-packages.outputs.packages-changed == 'true'
        uses: actions/cache@v4
        with:
          path: /var/cache/apt
          key: ${{ runner.os }}-apt-packages
          restore-keys: |
            ${{ runner.os }}-apt-

      - name: Install system dependencies
        if: needs.check-packages.outputs.packages-changed == 'true'
        run: |
          sudo apt-get update
          sudo apt-get -y install --no-install-recommends xvfb libgtk-3-dev libgbm-dev libnotify-dev libnss3 libxss1 libasound2t64 libxtst6 xauth

      - name: Increase file watcher limit
        if: needs.check-packages.outputs.packages-changed == 'true'
        run: sudo sysctl fs.inotify.max_user_watches=1048576

      - name: Cache node_modules
        if: needs.check-packages.outputs.packages-changed == 'true'
        uses: actions/cache@v4
        id: node-modules-cache
        with:
          path: |
            node_modules
            **/node_modules
          # Match Semaphore cache strategy: branch-specific with yarn.lock hash, then branch-specific, then master
          key: ${{ runner.os }}-node-modules-${{ github.ref_name }}-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-node-modules-${{ github.ref_name }}-
            ${{ runner.os }}-node-modules-master-
            ${{ runner.os }}-node-modules-

      - name: Cache Cypress
        if: needs.check-packages.outputs.packages-changed == 'true'
        uses: actions/cache@v4
        id: cypress-cache
        with:
          path: ~/.cache/Cypress
          # Match Semaphore: branch-specific cache
          key: ${{ runner.os }}-cypress-${{ github.ref_name }}
          restore-keys: |
            ${{ runner.os }}-cypress-master
            ${{ runner.os }}-cypress-

      - name: Cache Lerna
        if: needs.check-packages.outputs.packages-changed == 'true'
        uses: actions/cache@v4
        id: lerna-cache
        with:
          path: |
            .lerna
            **/.lerna
          key: ${{ runner.os }}-lerna-${{ github.ref_name }}-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-lerna-${{ github.ref_name }}-
            ${{ runner.os }}-lerna-master-
            ${{ runner.os }}-lerna-

      - name: Install dependencies
        if: needs.check-packages.outputs.packages-changed == 'true' && (steps.node-modules-cache.outputs.cache-hit != 'true' || steps.cypress-cache.outputs.cache-hit != 'true' || steps.lerna-cache.outputs.cache-hit != 'true')
        run: yarn install --frozen-lockfile

      - name: Bootstrap Lerna
        if: needs.check-packages.outputs.packages-changed == 'true'
        run: npx lerna bootstrap

      - name: Cypress
        if: needs.check-packages.outputs.packages-changed == 'true'
        run: yarn cypress:ci
