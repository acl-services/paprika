nodeLinker: node-modules

# Yarn 4 disables dependency install scripts by default. Cypress (binary
# download), fsevents, @parcel/watcher and core-js all need theirs, so keep the
# Yarn 1 behaviour. Narrowing this to a per-package allowlist
# (dependenciesMeta.<pkg>.built) is a follow-up.
enableScripts: true
